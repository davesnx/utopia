  $ utopia --help
  utopia <command> [options]
  
  Commands:
    build      Build production artifacts
    export     Build and render static output
    dev        Start development workflow
    prod       Run built server in production mode (alias: start)
    clean      Remove generated artifacts
    info       Print environment and project metadata
  
  Shortcuts work as executable aliases, e.g. utopia-build.
  
  Global flags:
    -h, --help      Show this help
    -v, --version   Print CLI version

  $ utopia -h
  utopia <command> [options]
  
  Commands:
    build      Build production artifacts
    export     Build and render static output
    dev        Start development workflow
    prod       Run built server in production mode (alias: start)
    clean      Remove generated artifacts
    info       Print environment and project metadata
  
  Shortcuts work as executable aliases, e.g. utopia-build.
  
  Global flags:
    -h, --help      Show this help
    -v, --version   Print CLI version

  $ utopia --version
  0.1.0

  $ utopia -v
  0.1.0
