(library
 (name pages)
 (libraries utopia server-reason-react.react)
 (preprocess
  (pps server-reason-react.ppx)))

; (rule
; (deps markdown.md)
; (target markdown.re)
; (action
; (with-stdout-to
; %{target}
; (with-stdin-from
; %{deps}
; (run %{bin:utopia.markdown})))))
